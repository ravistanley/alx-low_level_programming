#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always returns 0 (Success)
 */

int main(void)
{
	int numberz;

	for (numberz = 0; numberz < 10; numberz++)
	{
		putchar(numberz);
	}

	putchar('\n');
	return (0);
}
